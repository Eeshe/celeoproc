services:
  db:
    image: postgres:18.6
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql
    environment:
      POSTGRES_DB: celeoproc
      POSTGRES_USER: celeoproc
      POSTGRES_PASSWORD: password

volumes:
  pgdata:
